# definindo a funçao
def divisores(n: int) -> int:
  cont = 0
  for i in range (1, n + 1):
    if n % i == 0 and i % 2 == 0:
      cont += 1
  return (cont)

# entrada do usuario
n = int(input())

# saida
print(divisores(n))
